all:
  hosts:
    openclaw:
      ansible_host: "{{ deploy_host }}"
      ansible_user: anvil
      ansible_ssh_private_key_file: ~/.ssh/id_ed25519
  vars:
    deploy_path: /apps/openclaw
    data_path: /apps/openclaw/data
